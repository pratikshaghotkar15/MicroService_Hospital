server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/NurseService
    username: postgres
    password: Prati@13

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: NURSE-SERVICE



eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
